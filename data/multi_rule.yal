(* Multiple rules test *)
{
import "fmt"
}

let digit = ['0'-'9']
let letter = ['a'-'z' 'A'-'Z']

rule token1 =
  digit+ { return NUM }
| letter+ { return ID }

rule token2 =
  '+' { return PLUS }
| '-' { return MINUS }
| '*' { return TIMES }

{
func helper() { return }
}
