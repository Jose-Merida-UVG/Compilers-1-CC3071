iftrue elsewhere whileloop forklift returning integer boolean truefalse falsetto
if else while for return int bool true false
x == y != z <= w >= v && a || b !flag
foo(bar, baz) { result = 1 + 2.5 * 3 / 4 % 2 }
ifx elsey whilez forw returnz intz boolz truez falsez
